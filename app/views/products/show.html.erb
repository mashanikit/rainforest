  <h2><%= link_to @product.name, product_path(@product) %></h2>
  <p><%= @product.description %></p>
  <p><%= @product.price_in_dollars %></p>



<%= link_to("Edit", "/products/#{@product.id}/edit") %> | <%= link_to("Delete", "/products/#{@product.id}", method: :delete, data: {confirm: "Are you sure you want to delete this product?"})  %>
